# Copy this file to .env.local and fill in real values.

# Laravel API (see docs/api-contract.md). Leave unset to run entirely
# against the built-in in-memory mock (src/lib/api/mockAdapter.ts) — no
# backend needed at all for local frontend dev. Example:
# http://localhost:8000/api/v1
NEXT_PUBLIC_API_URL=
